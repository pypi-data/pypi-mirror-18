server 
{
    listen ${port};
    server_name ${hostname};

    root ${prefix}/var/www;      
    index index.html index.htm;

    # WPS Outputs
    location /wpsoutputs
    {
        alias ${prefix}/var/lib/pywps/outputs;
        autoindex off;
    }

}
