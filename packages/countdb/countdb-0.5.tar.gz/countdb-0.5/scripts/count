#!/usr/bin/env python
from __future__ import print_function

import sys

from countdb import CountDB

filename = sys.argv[1]

with CountDB.open_for_counting(filename) as db:
    for line in sys.stdin.readlines():
        db.count(line.strip())
