#!/bin/bash

set -e

appPath="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Generating minified CSS..."

curl -s \
    --form "source=@$appPath/www/static/global.less" \
    minify.pelicandd.com/api/v1/less \
    > "$appPath/www/static/g.min.css"
